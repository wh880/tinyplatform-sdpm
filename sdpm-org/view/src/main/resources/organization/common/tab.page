#foreach(m in menuManager.getChildMenus("organization"))
    #if(m.scope != "org-menu")
        #@aHeadCol(TINY_CONTEXT_PATH+adminPath+m.href m.id==name?"Current":"")${m.name}#end
    #end
#end

<div style="float: right">
    #foreach(m:menuManager.getScopeMenus("org-menu"))
        <a href="${TINY_CONTEXT_PATH+adminPath}${m.href}">#buttonIcon(iconClass=m.icon)${m.name}</a>
    #end
</div>

##    <script type="text/javascript">
##            $(function () {
##                $("[id=organizationCompany]").on("change", function () {
##                    $("input[name=taskAssignedDate]").val(moment().format('YYYY-MM-DD'));
##                });
##            });
##</script>