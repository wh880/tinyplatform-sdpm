

#setTinyItemCount(docpager.getTotalCount())
${docpager.getTotalCount()}
##for(var:[1..pagesize])
#foreach(l:docpager.getRecords())
#@tr()
	#@td()<input type="checkbox" value="1" name="idCheck">#end
	#@td()<a href="${TINY_CONTEXT_PATH}/document/doc/view/${l?.docId}">${l?.docId}</a>#end
	#@td()<a href="${TINY_CONTEXT_PATH}/document/doc/view/${l?.docId}">${l?.docTitle}</a>#end
	#@td()${l?.getDocType()}#end
	#@td()${l?.getDocAddedBy()}#end
	#@td()${l?.getDocAddedDate()}#end
	 #@td()
	 	#foreach(m:menuManager.getScopeMenus("doc-op"))
			#if(m.id != "docdelete")
				<a href="${TINY_CONTEXT_PATH}${m.href}/${l?.docId}" title="${m.name}">#buttonIcon(iconClass=m.icon)</a>
			#else
				#@tinyTableItemRemove(TINY_CONTEXT_PATH+m.href l.docId)
					<i class="${m.icon}" title="删除"></i>
				#end
			#end
		#end
#*	 	<a href="${TINY_CONTEXT_PATH}/document/table-edit" title="编辑">#buttonIcon(iconClass="icon-pencil")</a>	    
		#iconOperation(" icon-remove" "删除" "ml-5")
		#@tinyTableItemRemove("/tinyuiweb/action/del.pagelet" var)
								<i class="icon-remove"></i>
							#end*#
	#end
#end
#end

#@dialog("myModal")
	#@dialogHeader()对话框标题#end
	#@dialogBody()<p>对话框内容…</p>#end
	#@dialogFooter()
		#@buttonEnsure()确定#end
		#@buttonCancel()关闭#end
	#end
#end