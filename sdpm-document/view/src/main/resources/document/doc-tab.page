#@div()
    #@tabWithInit("tab_demo" "optionsBox")
        #@div(divClass="dropdownBox")
            #include("/document/dropdownMenu.page")
        #end
        #@tabHead()
            #include("tab.page")          
        	<span class="r" style="position:relative;top:2px;height:0">
        		#foreach(m:menuManager.getScopeMenus("doclib-op"))
					#if(m.id == "doclib-edit")
						#@aModal("#myModal" m.name "400" "120" "class-m" adminPath+m.href)
        	        		#buttonIcon(iconClass=m.icon)${m.name}#end&nbsp
        	       	#else
        	       		#if(m.id == "doclib-delete")
        	       			 #@delItemsdpm(TINY_CONTEXT_PATH+adminPath+m.href documentLibId TINY_CONTEXT_PATH+adminPath+"/document")
								#buttonIcon(iconClass=m.icon)${m.name}#end&nbsp	
						#else	
							#@aModal("#myModal" m.name "400" "120" "class-m" m.href)
								#buttonIcon(iconClass=m.icon)${m.name}#end			
        	       		#end
					#end
				#end					
			</span>
        #end
        #@tabBodyCol("ptop-5")#end
     #end
#end
#@dialog("myModal")
	#@dialogHeader()对话框标题#end
	#@dialogBody()<p></p>#end
	#@dialogFooter()
	#end
#end