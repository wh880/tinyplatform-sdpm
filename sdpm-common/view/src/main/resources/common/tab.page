#!set(activeTitle="选项卡",activeMenuId=currentPageId)
#@tabWithInit("tab_demo")
    #@tabHead()
        #@tabHeadCol()选项卡一#end
        #@tabHeadCol()选项卡二#end
    #end
    #@tabBodyCol("ptop-5")
    <ul class="tabOption">
        <li class="tabCurrent">选项卡一</li>
        <li>选项卡二</li>
    </ul>
    <div class="tabBoxs">
        <ul style="display:block;">
            <li>内容存放位置</li>
        </ul>
        <ul>
            <li>显示想放的具体内容啊</li>
        </ul>
    </div>
    #end
    #@tabBodyCol("ptop-5")
    这里所放位置
    #end
#end
<script type="text/javascript">
    $(function () {
        var timeid;
        $(".tabOption").find("li").each(function (index) {
            var sTab = $(this);
            sTab.mouseenter(function () {
                timeid = setTimeout(function () {
                    sTab.addClass("tabCurrent").siblings().removeClass("tabCurrent");
                    sTab.parent().siblings().find("ul:eq(" + index + ")").show().siblings().hide();
                }, 300);
            }).mouseleave(function () {
                clearTimeout(timeid);
            })
        })
    })
</script>