#import("/org/tinygroup/fold/components/fold.component")
#@foldMenu("" "Tinyfold3" "" "width:100%")
    #for(a : actionMap)
        #@foldItem( formatDate(a?.key?.actionDate,"yyyy-MM-dd HH:mm:ss")+" 由 "+a?.key?.actorName+" "+getDict("actionOperate",a?.key?.actionAction)+" " )
            #if(a?.value?.size()>0)
        记录详情<br>
        #end
            #for(h : a?.value)
            <p>修改了<b>${getStd(a?.key?.actionObjectType,h?.historyField)}</b>旧值为 ${getDict(h?.historyField,h?.historyOld)?:h?.historyOld}，新值为 ${getDict(h?.historyField,h?.historyNew)?:h?.historyNew}。</p>
            #end
            <br>
            ${a?.key?.actionComment}
        #end
    #end
#end
<script type="text/javascript">
$(function(){
	$(".Hui_aside").scrollTop(380);
	/*折叠*/
	/*5个参数顺序不可打乱，分别是：相应区,隐藏显示的内容,速度,类型,事件*/
	$.Tinyfold("#Tinyfold3 .item h4","#Tinyfold3 .item .info","",3,"click");
});
</script>