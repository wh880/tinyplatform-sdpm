#include("/system/page/event/common.page")

#setTinyItemCount(holiday.totalCount)
#for(h:holiday.records)
#@tr()
	#@td()<input type="checkbox" value="${h?.holidayId}" name="idCheck">#end
	#@td()${h?.holidayId}#end
	#@td()
		<a title="国庆节"  href="${TINY_CONTEXT_PATH}/system/page/holiday/edit.page">${h?.holidayName}</a>
	#end
	#@td()${h?.holidayDate}#end
	#@td()${h?.holidayAccount}#end
	#@td()${h?.holidayType}#end
	#@td()
		#foreach(m in menuManager.getScopeMenus("myholiday-op"))
			#if("myholiday-del"!=m.id)
				<a title="${m.name}" class="ml-5" href="${TINY_CONTEXT_PATH}${m.href}"+"?holidayId="+${h?.holidayId}><i class="${m.icon}"></i></a>
			#else
				#iconOperation(m.icon m.name "ml-5 delBtn"m.href+"?holidayId="+h?.holidayId)
			#end
			
		#end
	 	
	#end
	
#end
#end