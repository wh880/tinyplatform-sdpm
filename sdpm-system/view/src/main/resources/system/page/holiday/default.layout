#include("/system/page/event/common.page")
#set(linkName="holiday")

		#@div(divClass="operationbar")
			<span class="f">
				#foreach(m in menuManager.getChildMenus("holiday"))
						<a href="${TINY_CONTEXT_PATH}${m.href}" class="active_btn">${m.name}</a>
				#end
			</span>	
				
				
				#div(divClass="clearfix")
		#end
		
		#pageContent