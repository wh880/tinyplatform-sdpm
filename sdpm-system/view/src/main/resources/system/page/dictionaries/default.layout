#include("/system/page/event/common.page")

<style>

 
</style>

#set(linkName="dictionaries")

		#@div(divClass="operationbar")
			<span class="f">
				#foreach(m in menuManager.getChildMenus("dictionaries"))
						<a href="${TINY_CONTEXT_PATH+adminPath}${m.href}" class="${m.id==threeMenu?'active_btn':''}">${m.name}</a>
				#end
			</span>	
				
				
				#div(divClass="clearfix")
		#end
	 #@tinyForm("dictEditform" "" "post" {"class":""}) 
		#@topSearch() 
		    #simpleText("moduleName" "" "width:25%")
		    #simpleText("中文名" "" "width:25%")
		 	#@btnSubmit("icon-search") 搜索#end
		 	
		#end
		#end
		
		#pageContent