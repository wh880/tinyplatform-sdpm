
#set(records=productLine?.totalCount,temp=1,pagesize=temp.parseInt(pagesize),page=temp.parseInt(page),pagesize=pagesize<100?pagesize:100)*#
#if (pagesize>records-pagesize*(page-1))
#set(pagesize=records-(page-1)*pagesize)
#end
#setTinyItemCount(productLine?.totalCount)
#for(productLine:productLine?.records)
#@tr()
	#@td()<input type="checkbox" value="1" name="productLine">#end
	#@td()${productLine?.productLineId}#end
	#@td()
		<a href="${TINY_CONTEXT_PATH +adminPath}/productLine/find/overview?productLineId=${productLine?.productLineId}">${productLine?.productLineName}</a>
#end
	#@td()${productLine?.productLineCode}#end
	#@td()${productLine?.companyId}#end
	#@td()${productLine?.deptId}#end
	#@td()${productLine?.productLineRoot}#end
	#@td()${productLine?.productLineParent}#end
	#@td()${productLine?.productLineOwner}#end
	#@td()${productLine?.productLineQualityManager}#end
	#@td()${productLine?.productLineDeliveryManager}#end
	#@td()${productLine?.productLineStatus}#end
	#@td()
			#foreach(m:menuManager.getScopeMenus("productLine-all"))
			#set(url=eval(m.href))
				#if(m.id=="allproductline-edit")
					<a title="${m.name}" class="ml-5" href=${TINY_CONTEXT_PATH +adminPath}${url}><i class="${m.icon}"></i></a>
					#else
					&nbsp;#@tinyTableItemRemove(TINY_CONTEXT_PATH +adminPath+url productLine.productLineId)<i class="${m.icon}"></i>#end
				#end
			#end
	#end
#end
#end