#set(records=productPlan.totalCount,temp=1,pagesize=temp.parseInt(pagesize),page=temp.parseInt(page),pagesize=pagesize<100?pagesize:100)
#if (pagesize>records-pagesize*(page-1))
#set(pagesize=records-(page-1)*pagesize)
#end
#setTinyItemCount(productPlan.totalCount)
#for(plan:productPlan.records)
#@tr()
	#@td()<input type="checkbox" value="1" name="product-plan">#end
	#@td()${plan?.planId}#end
	#@td()<a href="${TINY_CONTEXT_PATH}/product/page/tabledemo/relation-plan/product-al-req.page">${plan?.planName}</a>#end
	#@td()${plan?.planSpec}#end
	#@td()${formatDate(plan?.planBeginDate,'yyyy年MM月dd日')}#end
	#@td()${formatDate(plan?.planEndDate,'yyyy年MM月dd日')}#end
	 #@td()
		 #foreach(m:menuManager.getScopeMenus("product-pl"))
			 #if(m.id!="product-plan-remove")
             <a title="${m.name}" class="ml-5" href="${TINY_CONTEXT_PATH}${m.href}"><i class="${m.icon}"></i></a>
				 #else
				 #iconOperations("#myModal" m.icon m.name "865" "500" url "ml-5")&nbsp;
#*				&nbsp; #@tinyTableItemRemove("#" "id")<i class="icon-remove"></i>#end*#
			 #end
		 #end
	#end
#end
#end

