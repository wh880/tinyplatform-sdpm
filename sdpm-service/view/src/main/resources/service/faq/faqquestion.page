#@OlTopList()

    #foreach(l:pager.records)
    #*#@normalLiItemsconment(1 "../faq/questionAnswer.page" l.faqQuestion "")*#
        #@normalLiItemsconment(1 "../faq/questionAnswer?id="+l.faqId l.faqQuestion "")


        #*faqmenu问题后面的编辑和删除*#
            #set(m1=menuManager.getMenu("faq-edit"))
            #if(m1)
            #*<a href="${TINY_CONTEXT_PATH}${m1.href}?id=${var}">#buttonIcon(iconClass="icon-pencil")${m1.name}</a>*#
            <a href="${TINY_CONTEXT_PATH}${m1.href}?id=${l.faqId}">#buttonIcon(iconClass="icon-pencil")${m1.name}</a>
            #end
            #set(m2=menuManager.getMenu("faq-del"))
            #if(m2)
            #*#@delItemsdpm(TINY_CONTEXT_PATH+m2.href l.faqId m2.href {"class":m2.icon})${m2.name}#end*#
                #@delItemsdpm(TINY_CONTEXT_PATH+m2.href l.faqId "../faq/list"{"class":m2.icon})${m2.name}#end
            #end
            #@dialog("myModal")
                #@dialogHeader()对话框标题#end
                #@dialogBody()<p>对话框内容…</p>#end
                #@dialogFooter()
                    #@buttonEnsure()确定#end
                    #@buttonCancel()关闭#end
                #end
            #end
        #end
    #end
#####最后一行为问题前面的颜色   #@normalLiItemsconment(2 "../page/questiondb.page" "项目里面的版本和测试的版本有什么区别？" "green")

#end