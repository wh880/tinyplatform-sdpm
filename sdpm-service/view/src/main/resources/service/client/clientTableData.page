#setTinyItemCount(pager?.totalCount)
#foreach(var:pager?.records)
    #@tr()
        #@td()<input type="checkbox" value="${var.clientId}" name="idCheck">#end
    ##        #@td()<a href="${TINY_CONTEXT_PATH+adminPath}/service/client/clientSla?id=${var.clientId}">${var.clientId}</a>#end
        #set(m=menuManager.getMenu("client-sla"))
        #if(m)
            #@td()<a
                    href="${TINY_CONTEXT_PATH+adminPath}/service/client/clientSla?id=${var.clientId}">${var.clientName}</a>#end
        #end
        #@td()${var.clientNO}#end
        #@td()${var.clientCreatedBy}#end
        #@td()${formatDate(var.clientCreateDate,"yyyy-MM-dd")}#*${var.clientCreateDate}*##end
    #*#@td()${var.clientStatus}#end*#
        #@td()${getDict("clientStatus",var?.clientStatus)}#end
        #@td()
            #foreach(m:menuManager.getScopeMenus("service-cil"))
                #if(m.id != "client-del")
                <a href="${TINY_CONTEXT_PATH+adminPath}${m.href}?id=${var.clientId}"
                   title="${m.name}">#buttonIcon(iconClass=m.icon)</a>
                #else
                    #@tinyTableItemRemove(TINY_CONTEXT_PATH+adminPath+m.href var.clientId)<i class=${m.icon}></i>#end
                #end
            #end
        #end
    #end
#end
