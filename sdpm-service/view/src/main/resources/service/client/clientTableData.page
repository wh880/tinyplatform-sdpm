#macro setTinyItemCount(num)
<input type="hidden" name="records" value="${num}"/>
#end
#setTinyItemCount(pager.totalPages)
#foreach(var:pager.records)
    #@tr()
        #@td()<input type="checkbox" value="${var.clientId}" name="idCheck">#end
        #@td()<a href="${TINY_CONTEXT_PATH}/service/client/clientProduct.page">${var.clientId}</a>#end
        #@td()<a href="${TINY_CONTEXT_PATH}/service/client/clientInfo.page">${var.clientName}</a>#end
        #@td()${var.clientDeptId}#end
        #@td()${var.clientCreatedBy}#end
        #@td()${var.clientCreateDate}#end
        #@td()${var.clientStatus}#end
        #@td()
            #foreach(m:menuManager.getScopeMenus("service-cil"))
                #if(m.id != "client-del")
                   #* #iconOperations("#myModal" m.icon m.name "650" "350" m.href+"?id="+var "ml-5")*#
                <a href="${TINY_CONTEXT_PATH}${m.href}?id=${var.clientId}"
                   title="${m.name}">#buttonIcon(iconClass=m.icon)</a>
                #else
                    #@tinyTableItemRemove("#" "id")<i class="${m.icon}"></i>#end
                #end
            #end
        #end
    #end
#end
