#!set(activeTitle="项目管理",activeMenuId=currentPageId)
#import("/org/tinygroup/form/components/form.component")
#import("/org/tinygroup/button/components/button_common.component")
#import("/org/tinygroup/button/components/button.component")
#import("/org/tinygroup/table/components/table.component")

#set(name="survey")
#@div()
    #hiddenInput(hiddenInputName="projectId" hiddenInputValue=project?.projectId)
    #@tabWithInit("tab_demo" "optionsBox")
        #@div(divClass="dropdownBox")
            #include("/project/dropdownMenu.page")
        #end
        #@tabHead()
            #include("/project/protab.page")
        #end
        #@tabBodyCol("ptop-5")
            #@div(divClass="operationbar")
            <span class="f">
                ${selProject.projectName}
            </span>
            <span class="r">
				#set(m1=menuManager.getMenu("pro-survey-delay"))
                #if(m1)

                    #@aModal(
                        "#m" selProject.projectName+">延期" "750" "350" "" adminPath+
                        "/project/delay?projectId="+selProject.projectId)#buttonIcon(iconClass="icon-calendar")延期
                    #end
                #end
                #set(m2=menuManager.getMenu("pro-survey-hangon"))
                #if(m2)
                    #@aModal(
                        "#m" selProject.projectName+">挂起" "750" "250" "" "/project/survey/hang-up.pagelet")
                        #buttonIcon(iconClass="icon-pause")挂起
                    #end

                #end
                #set(m3=menuManager.getMenu("pro-survey-finish"))
                #if(m3)
                    #@aModal(
                        "#m" selProject.projectName+">结束" "750" "250" "" "/project/survey/hang-up.pagelet")
                        #buttonIcon(iconClass="icon-off")结束
                    #end

                #end

                #foreach(m:menuManager.getScopeMenus("pro-surveymenu2-op"))
                    <a href="${TINY_CONTEXT_PATH+adminPath}${m.href}?projectId=${project?.projectId}"
                       title="${m.name}">#buttonIcon(iconClass=m.icon)</a>
                #end

                #set(m4=menuManager.getMenu("pro-survey-remove"))
                #if(m4)
                    #@aModal("#m" "提示" "300" "50")#buttonIcon(iconClass="icon-remove")#end

                #end

				</span>
                #div(divClass="clearfix")
            #end
        #end


    #end
#end
<div class="infoPage-left">
    #@div(divClass="infoContent")
        <p class="titleName">
            项目描述
        </p>

        <div>
            ${selProject.projectDesc}
        </div>
    #end
    #@div(divClass="infoContent")
        #history(adminPath+"/system/action/ajax/history?actionObjectType=project&actionObjectId="+selProject.projectId)
    #end
</div>
<div class="infoPage-right">
    #TypeInfo("project", selProject?.projectId)
</div>

