#import("/org/tinygroup/button/components/button_smart.component")
#import("/org/tinygroup/button/components/button_common.component")

#import("/org/tinygroup/button/components/button.component")

#!set(activeTitle="项目管理",activeMenuId=currentPageId)


#!set(linkName="release",sonitem="
<li>"+"发布"+"</li>")
#@div(divClass="operationbar")

<span class="r">
				#@aModal("#myModal" "新增日志" "1000" "350" "btn-class"
                    "/project/task/note.pagelet")#buttonIcon(iconClass="icon-time")日志#end
    <a href="${TINY_CONTEXT_PATH+adminPath}/project/version/forword/alReq?buildId=${build.buildId}">#buttonIcon(iconClass=
        "icon-link")关联需求</a>&nbsp;
				<a href="${TINY_CONTEXT_PATH+adminPath}/project/version/forword/alBug?buildId=${build.buildId}">#buttonIcon(iconClass=
                    "icon-bug")关联Bug</a>&nbsp;
<a href="${TINY_CONTEXT_PATH+adminPath}/project/version/edit?buildId=${b?.buildId}">#buttonIcon(iconClass=
    "icon-pencil")编辑发布</a>&nbsp;
        <a href="${TINY_CONTEXT_PATH+adminPath}/project/version/delete?id=${build?.buildId}"
           name="删除发布">#buttonIcon(iconClass=
            "icon-remove")</a>&nbsp;
    <a href="${TINY_CONTEXT_PATH+adminPath}/projectmanage/build/index" title="返回">#buttonIcon(iconClass=
        "icon-reply")</a>
				</span>#div(divClass="clearfix")

#end


#@tabWithInit("tab_demo" "optionsBox")
<div style="width:70%;float:left">
    #@div()
        #@tabWithInit("tab_demo" "optionsBox")
            #@tabHead()
                #if("demand"==type)
                    #@aHeadCol(TINY_CONTEXT_PATH+adminPath+
                        "/project/version/forword/alReq?buildId="+build.buildId cname==
                        "demand"?
                        "Current":"")
                        #@buttons("","icon-lightbulb")
                            已完成需求
                        #end
                    #end
                    #@aHeadCol(TINY_CONTEXT_PATH+adminPath+
                        "/project/version/forword/alnoReq?buildId="+build.buildId cname==
                        "no-demand"?"Current":"")
                        #@buttons("","icon-unlink")
                            未关联需求
                        #end
                    #end
                    #if("no-demand"==cname)
                        <div style="float:right">
                            <a href="#" id="showsearchmore">#buttonIcon(iconClass="icon-search")搜索</a>
                        </div>
                    #end
                #end

                #if("bug"==type)
                    #@aHeadCol(TINY_CONTEXT_PATH+adminPath+
                        "/project/version/forword/alBug?buildId="+build.buildId cname=="bug"?
                        "Current":"")
                        #@buttons("","icon-bug green")
                            已解决Bug
                        #end
                    #end
                    #@aHeadCol(TINY_CONTEXT_PATH+adminPath+
                        "/project/version/forword/alnoBug?buildId="+build.buildId cname==
                        "no-bug"?"Current":"")
                        #@buttons("","icon-unlink")
                            未关联Bug
                        #end
                    #end
                    #@aHeadCol(TINY_CONTEXT_PATH+adminPath+
                        "/project/version/forword/alleBug?buildId="+build.buildId cname==
                        "le-bug"?"Current":"")
                        #@buttons("","icon-bug green")
                            遗留Bug
                        #end
                    #end
                    #if("no-bug"==cname)
                        <div class="r">
                            <a href="#" id="showsearchmore">#buttonIcon(iconClass="icon-search")搜索</a>
                        </div>
                    #end
                #end

                <div class="r">
                    #@aModal(
                        "#myModal" "导出HTML" "700" "130" "icon-upload-alt"
                        "/project/task/relation-release/product-export-html.pagelet")
                        导出HTML#end&nbsp;
                </div>
            #end
            #@tabBodyCol("ptop-5")
                #pageContent
            #end
        #end
    #end
</div>
<div style="width:30%;float:right">
    #TypeInfo("build",build?.buildId)
</div>
#end