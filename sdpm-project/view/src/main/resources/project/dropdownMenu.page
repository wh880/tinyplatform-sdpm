#@navSelect2("sleProject" "sleProject")
    #@navSelect2Nav()
        #if(selProject)
            ${selProject.projectName}
        #end
    <span class="caret"></span>
    #end
    #@navSelect2Body()
        #@navSelect2Data()
        <option value=""></option>
        <optgroup label="项目">
            #for(p:projectList)
            <option value="${p?.projectId}">${p?.projectName}</option>
        #end
    </optgroup>
    #end
    #@navSelect2Fix()
    <a href="${TINY_CONTEXT_PATH+adminPath}/project/preadd">#buttonIcon(iconClass="icon-plus-sign")添加项目</a>
    #end
    #@dialog("myModal")
        #@dialogHeader()对话框标题#end
        #@dialogBody()<p>对话框内容…</p>#end
        #@dialogFooter()
        #end
    #end
#end
#end
<script type="text/javascript">
    $(function () {
        $("#sleProject-select").on("change", function (e) {
        ##            window.location.href = "${TINY_CONTEXT_PATH+adminPath}/project/task/index.page?selItem="
        ##                    + $(this).find("option:selected").text() + "&itemid=" + $(this).val() + "&stat=" + $();
        ##            //val() 选择选择项的value
            window.location.href = "${TINY_CONTEXT_PATH+adminPath}/project/manage/select?projectId="
                    + $(this).find("option:selected").val()
                    + "&oldUrl=${tinyRequestRequestURI.substring(9)}";
            console.log("ggg");
        });
    });
</script>