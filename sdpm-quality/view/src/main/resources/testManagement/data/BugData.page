#macro setTinyItemCount(num)
<input type="hidden" name="records" value="${num}"/>
#end



#set(records=bugpager.totalCount,temp=1,pagesize=temp.parseInt(pagesize),page=temp.parseInt(page),pagesize=pagesize<100?pagesize:100)
#if (pagesize>records-pagesize*(page-1))
    #set(pagesize=records-(page-1)*pagesize)
#end
#setTinyItemCount(bugpager.totalCount)

#for(var:bugpager.records)
#@tr()
    #@td()<input type="checkbox" value="${var?.bugId}" name="bugId">#end
    #@td()<a href="${TINY_CONTEXT_PATH+adminPath}/testManagement/page/bugInfo.page">${var?.bugId}</a>#end
    #@td()${var?.bugSeverity}#end
    #@td()${var?.priority}#end
    #@td()<a href="${TINY_CONTEXT_PATH+adminPath}/testManagement/page/bugInfo.page">[${var?.bugConfirmed==1?"已确认":"未确认"}]${var?.bugTitle}</a>#end
    #@td()${getDict("bugStatus",var?.bugStatus)}#end
    #@td()${var?.bugOpenedBy}#end
    #@td()${formatDate(var?.bugOpenedDate,"yyyy-MM-dd")}#end
    #@td()${var?.bugAssignedTo}#end
    #@td()${var?.bugResolvedBy}#end
    #@td()${var?.bugResolution}#end
    #@td()${formatDate(var?.bugResolvedDate,"yyyy-MM-dd")}#end
    #@td()
    	#foreach(m:menuManager.getScopeMenus("testbug-op"))
             	<a href="${TINY_CONTEXT_PATH+adminPath}${m.href}?bugId=${var?.bugId}" title="${m.name}">#buttonIcon(iconClass=m.icon)</a>			
		 #end
    ##"icon-pencil"
    ##buttonRadiusLink(caption="/testManagement/page/proposeBug")
    #end
#end
#end

